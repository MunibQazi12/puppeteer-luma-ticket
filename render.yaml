services:
  - type: web
    name: puppeteer-luma-ticket
    env: node
    plan: standard
    buildCommand: "npm install --legacy-peer-deps"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
